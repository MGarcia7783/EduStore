{
  "name": "backend",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "start": "npx json-server --watch db-store.json --host 0.0.0.0 --port 10000"
  },
  "dependencies": {
    "json-server": "^1.0.0-beta.3"
  }
}
